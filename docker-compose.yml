version: '3.8'

services:
  img2lottie:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: img2lottie
    ports:
      - "5177:5177"
    environment:
      - NODE_ENV=production
      - PORT=5177
    restart: unless-stopped
    volumes:
      - img2lottie-data:/data

volumes:
  img2lottie-data:
